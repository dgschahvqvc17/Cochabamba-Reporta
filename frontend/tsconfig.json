{
  "extends": "@react-native/typescript-config",
  "compilerOptions": {
    // HU07: react-native-image-picker (src/platforms/web.ts) usa tipos DOM
    // (Blob, File, MediaStream…). El base RN no los incluye.
    "lib": ["ESNext", "DOM"]
  },
  "include": ["**/*.ts", "**/*.tsx"],
  "exclude": ["**/node_modules", "**/Pods"]
}
